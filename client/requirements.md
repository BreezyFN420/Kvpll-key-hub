## Packages
date-fns | Date formatting for tables
recharts | Dashboard charts for stats visualization
framer-motion | Page transitions and smooth animations
lucide-react | Icons for the sidebar and UI elements

## Notes
Authentication uses a custom header 'x-admin-token' stored in localStorage.
API requests need to inject this header.
Dark mode is the default theme.
